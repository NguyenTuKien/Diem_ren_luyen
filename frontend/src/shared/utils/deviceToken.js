// Device token is now stored in an HttpOnly cookie.
// This module is kept for backward compatibility and can be removed later.

export function getDeviceToken() {
  return null;
}

export function setDeviceToken() {}

export function clearDeviceToken() {}
