spring:
  config:
    import:
#      - optional:file:.env[.properties]
      - optional:file:./backend/.env[.properties]
  main:
    banner-mode: "off"
    time-zone: Asia/Ho_Chi_Minh
  application:
    name: backend
  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:aws-1-ap-southeast-1.pooler.supabase.com}:${POSTGRES_PORT:6543}/${POSTGRES_DB:test1}?prepareThreshold=0
    username: ${POSTGRES_USER:postgres.xhuvxyfehnsxnwljwtwq}
    password: ${POSTGRES_PASSWORD:Matkhau@1234qwer!}
    driver-class-name: org.postgresql.Driver
    hikari:
      max-lifetime: ${DB_HIKARI_MAX_LIFETIME_MS:900000}
      idle-timeout: ${DB_HIKARI_IDLE_TIMEOUT_MS:600000}
      keepalive-time: ${DB_HIKARI_KEEPALIVE_TIME_MS:120000}
      connection-timeout: ${DB_HIKARI_CONNECTION_TIMEOUT_MS:30000}
      validation-timeout: ${DB_HIKARI_VALIDATION_TIMEOUT_MS:5000}
      minimum-idle: ${DB_HIKARI_MIN_IDLE:2}
      maximum-pool-size: ${DB_HIKARI_MAX_POOL_SIZE:10}

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: true
    properties:
      hibernate.jdbc.time_zone: Asia/Ho_Chi_Minh

  jackson:
    time-zone: Asia/Ho_Chi_Minh

  data:
    redis:
      host: ${REDIS_HOST:redis-18097.c276.us-east-1-2.ec2.cloud.redislabs.com}
      port: ${REDIS_PORT:18097}
      password: ${REDIS_PASSWORD:VFzqHirkG4WP7UzBFTI5Vwbqj0mUHo1F}

  rabbitmq:
    host: ${RABBITMQ_HOST:capybara.lmq.cloudamqp.com}
    port: ${RABBITMQ_PORT:5671}
    username: ${RABBITMQ_USERNAME:zkwyimih}
    password: ${RABBITMQ_PASSWORD:V8gv8wLhBgYzvvTiQQaEeKp1LQbZAZYy}
    virtual-host: ${RABBITMQ_VIRTUAL_HOST:zkwyimih}
    ssl:
      enabled: ${RABBITMQ_SSL_ENABLED:true}

  security:
    oauth2:
      client:
        registration:
          microsoft:
            client-id: ${MICROSOFT_CLIENT_ID}
            client-secret: ${MICROSOFT_CLIENT_SECRET}
            redirect-uri: ${OAUTH2_REDIRECT_URI:{baseUrl}/login/oauth2/code/{registrationId}}
            scope:
              - openid
              - email
              - profile
              - User.Read
            authorization-grant-type: authorization_code
            client-name: Microsoft
        provider:
          microsoft:
            authorization-uri: https://login.microsoftonline.com/common/oauth2/v2.0/authorize
            token-uri: https://login.microsoftonline.com/common/oauth2/v2.0/token
            jwk-set-uri: https://login.microsoftonline.com/common/discovery/v2.0/keys
            user-info-uri: https://graph.microsoft.com/oidc/userinfo
            user-name-attribute: sub

  app:
    oauth2:
      redirect:
        url: ${OAUTH2_REDIRECT_URL:http://localhost:3000/oauth-success}

jwt:
  secret: ${JWT_SECRET:VGhpc0lzQURldlNlY3JldEtleUZvckpXVFNpZ25pbmdPbmx5ISE=}
  expiration-ms: ${JWT_EXPIRATION_MS:900000}
  refresh-expiration-ms: ${JWT_REFRESH_EXPIRATION_MS:604800000}

server:
  servlet:
    context-path: /api
  port: 8080
  forward-headers-strategy: framework

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:*}

logging:
  level:
    root: warn
    ct01.n06.backend: info

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always

